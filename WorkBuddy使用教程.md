# WorkBuddy 使用头条发布系统教程

> 这套头条自动化发布系统已与 WorkBuddy 深度集成：自动化任务已配置、脚本在 `E:\work\2026-08-16-16-58-28\output\`。
> 本教程教你**用 WorkBuddy 对话即可完成发布、管理、复盘**，不用碰命令行（进阶命令附在文末）。

---

## 一、快速开始：直接对话就能发

在 WorkBuddy 会话里说一句话，就能触发完整发布流程（选题→创作→配图→发布→记录）：

| 你想干嘛 | 对 WorkBuddy 说 |
|---------|----------------|
| 发一篇最新热点文章 | 「发一篇热点文章」 |
| 发防诈/民生类内容 | 「发一篇防诈类文章」「发一篇民生话题微头条」 |
| 指定主题 | 「写一篇关于【养老诈骗】的文章并发布」 |
| 今天收官 | 「发一篇今天就结束」「现在发一篇然后暂停」 |
| 看状态 | 「头条现在什么情况」「还能发几条」 |

WorkBuddy 会自动执行：限流检查 → 随机节奏 → 避开你手动发布 → 爆款选题（查重）→ 紧凑创作 → 契合配图 → 勾选广告收益 → 发布 → 记录。

## 二、自动化管理（每小时自动跑）

系统已配置每小时自动任务（automation-1786873643745），全部用对话控制：

| 指令 | 效果 |
|------|------|
| 「**暂停头条自动化**」 | 停止自动发布（调度保留） |
| 「**恢复头条自动化**」 | 重新开始每小时跑 |
| 「**取消头条自动化**」 | 彻底删除任务 |
| 「**看下自动化状态**」 | 汇报当前状态/下次触发时间 |
| 「**多发一点**」「**稳一点**」 | 调整发布频率（快/慢节奏） |

> 自动化每轮会过三道闸：限流红线（5min/3h/12h10/天20）→ 随机跳过 → 避开你手动发布。**宁可不发，绝不超限**。

## 三、数据与复盘

| 指令 | 结果 |
|------|------|
| 「**看收益**」 | 打开头条收益数据页，汇报昨日/本月/累计收益 |
| 「**回收数据**」 | 拉作品展现/阅读/点赞/评论，分析表现 |
| 「**看复盘**」 | 跑每日复盘，输出选题策略建议（优先/避免主题） |
| 「**今日发了什么**」 | 列出今天发布的内容列表 |

每日 0 点自动复盘（Python 调度器 + 计划任务/Trae），策略自动更新，WorkBuddy 选题时自动读取最新策略。

## 四、常见问题

| 问题 | 处理 |
|------|------|
| 提示未登录/要扫码 | 说「**头条需要登录**」，我打开浏览器你扫码 |
| 发布失败 | 说「**看下发布失败原因**」，我查错误截图定位 |
| 被限流了？ | 说「**查限流状态**」，我看今日/12h 发布计数 |
| 想改规则 | 直接说，如「以后多写民生类」「文章带标签」|

## 五、进阶：直接命令行（给想手动的你）

在终端（或让 WorkBuddy 执行）进入 `E:\work\2026-08-16-16-58-28\output`：

```bash
# 环境自检
toutiao-run.cmd env-check

# 发布微头条 / 文章
toutiao-run.cmd publish-toutiao --title "标题" --content "content.md" [--image "图.png"]
toutiao-run.cmd publish-article --title "标题" --content "article.md"

# 状态 / 查重 / 复盘
toutiao-run.cmd scheduler status
toutiao-run.cmd scheduler check-topic "热点标题"
python toutiao_scheduler.py report
python toutiao_scheduler.py daily
```

## 六、与 Trae 的配合

| | WorkBuddy | Trae |
|--|-----------|------|
| 角色 | 主阵地：自动定时 + 你日常操作 | 备用：出门在外临时发一条 |
| 定时 | ✅ 每小时自动化（已配好） | 需自配计划任务 |
| 指令 | 上面这些对话指令 | 用 `Trae头条发布提示词.md` |
| 数据 | 共享同一份 `scheduler_state.json` | 同上（两边都不会超发） |

**建议**：平时用 WorkBuddy 自动跑；Trae 只在你需要远程手动控制时用。两边限流状态共享，不会打架。
